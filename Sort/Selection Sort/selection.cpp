#include<iostream>
using namespace std;
#define MAX 1000
int main()
{   int arr[MAX], cnt,i,j,min,loc;
    cout<<"Enter Size Of Array :: ";
    cin>>cnt;
    cout<<"Enter Array Element ";
    for(int i=0;i<cnt;i++)
    {
        cin>>arr[i];
    }
    for(i=0;i<cnt-1;i++)
    {
        for(j=0;j<cnt;j++)
        {
            min=arr[j];
            loc=j;
            if(a[j]<a[j+1])
            {
                a[j]
            }
        }
    }
    return 0;
}